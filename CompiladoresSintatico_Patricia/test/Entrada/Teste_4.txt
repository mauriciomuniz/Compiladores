//codigo de um lexico para um compilador
const
{
    string tipos= "inteiro||real||boleano||cadeia";
    string palavra_reservada = "funcao||metodo||inteiro||real||boleano||cadeia"; 
}

procedure lex( ){  
 var{
        int automatos[100][100];
        boolean bloco,numero,numNeg,cadeia,erro;
        int contador_linha;
        int linhas_de_codigo;
        string arquivo[100];
        string tokens[100];
    }  
    write("Quantas linhas tem seu código?");
    read(linhas_de_codigo);
    
    for(contador_linha=0; contador_linha<=linhas_de_codigo; contador_linha++){
        if((arquivo[contador_linha] == tipo)){
            tokens[contador_linha] = arquivo(contador_linha);
        }else{
            if((arquivo[contador_linha] == palavra_reservada)){
                 tokens[contador_linha] = arquivo(contador_linha);
            }else{
                write("erro no lexico");
            }
        }
    }                          
}